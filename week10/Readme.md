# Задача 1
Напишете структурата за полиморфен свързан списък и следните функции:

- `head :: List a -> Maybe a` - връща първия елемент от списъка, ако съществува
- `tail :: List a -> Maybe (List a)` - връща опашката на списъка, ако съществува
- `head' :: List a -> Either String a` - връща първия елемент от списъка, ако съществува, иначе връща съобщение за грешка
- `tail' :: List a -> Either String (List a)` - връща опашката на списъка, ако съществува, иначе връща съобщение за грешка


# Задача 2
Напишете инстанции за `Semigroup` и `Monoid` за структурата `List a` от предишната задача.

- `instance Semigroup (List a)` - конкатенация на два списъка
- `instance Monoid (List a)` - празен списък като неутрален елемент
- `instance Show a => Show (List a)` - дефинирайте извеждането на `String`, като се поставят стрелки между всички елементи
# Задача 3
Напишете нов тип `EAI` който е обвивка около `String` и имплементирайте следните инстанции:

- `instance Semigroup EAI` - конкатенация на два стринга
- `instance Monoid EAI` - празен стринг като неутрален елемент


# Задача 4
Напишете функцията `allNats :: [[Integer]]` която генерира безкрайна поредица от всички възможни списъци от естествени числа.

Пример: `take 20 allNats` трябва да върне `[[1],[2],[0,1],[1,0],[3],[0,2],[1,1],[2,0],[0,0,1],[0,1,0],[1,0,0],[4],[0,3],[1,2],[2,1],[3,0],[0,0,2],[0,1,1],[0,2,0],[1,0,1]]`


# Задача 5
Напишете функцията `treesGen :: [Tree]` която генерира безкрайна поредица от всички възможни дървета от тип `Tree`.

Дървото е дефинирано като `data Tree = Tree Integer Tree Tree | Nil`, където `Integer` е стойността на възела.

# Задача 6
Напишете структурите за робот и команди, и функцията за изпълнение на команди:

- `data RobotState` - състояние на робот с позиция (X, Y, Z), ориентация (Rol, Yaw, Pitch) и име
- `data Comands` - команди за движение по осите (MoveX, MoveY, MoveZ), за въртене (Rol, Yaw, Pitch) и за смяна на името (ChangeName)
- `executeCommand :: RobotState -> Comands -> RobotState` - изпълнява една команда и връща новото състояние на робота


# Задача 7
Разширете предишната задача с допълнителни команди и функция за изпълнение на множество команди:

- Добавете команди за диагонално движение: `MoveDiaonalXY`, `MoveDiaonalXZ`, `MoveDiaonalZY`
- `executeMultipleCommands :: RobotState -> [Comands] -> RobotState` - изпълнява списък от команди последователно

Диагоналните команди трябва да използват `executeMultipleCommands` за изпълнение на съответните движения по отделните оси.


# Задача 8
Напишете програма която използва `do` нотация за следните операции:

- Чете първо число от конзолата
- Чете второ число от конзолата
- Извежда сумата на двете числа

Програмата трябва да използва `do` нотация и да извежда подходящи съобщения за всеки етап.


# Задача 9
Напишете инстанция за `Functor` за структурата `List a`:

- `instance Functor List` - прилага функция върху всички елементи на списъка

Пример: `fmap (+1) (List 1 (List 2 (List 3 Nil)))` трябва да върне `List 2 (List 3 (List 4 Nil))`


# Задача 10
Напишете инстанции за `Foldable` и `Functor` за структурата `List a`:

- `instance Foldable List` - имплементира `foldr`, `foldl`, `foldMap` за списъка
- `instance Functor List` - прилага функция върху всички елементи на списъка

